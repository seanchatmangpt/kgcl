#!/usr/bin/env bash
# Lightweight wrapper to ensure every git pre-commit invocation
# uses the unified KGCL quality gate implementation.

set -euo pipefail

REPO_ROOT="$(git rev-parse --show-toplevel 2>/dev/null || pwd)"
exec "$REPO_ROOT/scripts/git_hooks/pre_commit.sh" "$@"
