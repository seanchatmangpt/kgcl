# SPARQL Injection Architecture Diagram

**Visual Guide**: Understanding the pure kernel pattern through architecture diagrams.

---

## 📊 Component Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                        USER REQUEST                              │
│  copy(graph, subject=task123, ctx={...}, config=VerbConfig)     │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                     VERB CONFIG (Frozen Dataclass)               │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │ verb_uri: kernel:Copy                                     │  │
│  │ execution_template_uri: kernel:CopyTopologyTemplate ◄────┼──┼─── Points to RDF
│  │ parameters: {"cardinality": "topology"}                  │  │
│  │ timeout_ms: 100                                          │  │
│  └───────────────────────────────────────────────────────────┘  │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                   KERNEL.COPY() - IMMUTABLE                      │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │ Step 1: Retrieve template from ontology                  │  │
│  │   template = query(execution_template_uri)               │  │
│  │                                                           │  │
│  │ Step 2: Bind variables                                   │  │
│  │   bindings = {subject, **ctx, **parameters}              │  │
│  │                                                           │  │
│  │ Step 3: Execute SPARQL                                   │  │
│  │   results = graph.query(template, bindings)              │  │
│  │                                                           │  │
│  │ Step 4: Convert to QuadDelta                             │  │
│  │   return QuadDelta(additions=results)                    │  │
│  │                                                           │  │
│  │ ❌ NO if/else ❌ NO parameter interpretation             │  │
│  └───────────────────────────────────────────────────────────┘  │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                    RDF ONTOLOGY (Business Logic)                 │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │ kernel:CopyTopologyTemplate                               │  │
│  │   a kernel:ExecutionTemplate ;                            │  │
│  │   kernel:sparqlTemplate """                               │  │
│  │     SELECT ?predicate ?object WHERE {                     │  │
│  │       ?subject ?predicate ?object .                       │  │
│  │       FILTER(?predicate != rdf:type)                      │  │
│  │       FILTER(?predicate NOT IN (                          │  │
│  │         kernel:createdAt, kernel:updatedAt                │  │
│  │       ))                                                  │  │
│  │       FILTER EXISTS {                                     │  │
│  │         ?predicate rdfs:subPropertyOf*                    │  │
│  │                    kernel:structuralProperty .            │  │
│  │       }                                                   │  │
│  │     }                                                     │  │
│  │   """ .                                                   │  │
│  │                                                           │  │
│  │ ✅ ALL business logic ✅ Declarative ✅ Queryable        │  │
│  └───────────────────────────────────────────────────────────┘  │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                  SPARQL ENGINE (Operator μ)                      │
│  Executes template against graph with variable bindings         │
│  Returns: [(flowsInto, task456), (flowsInto, task789)]          │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                   QUAD DELTA (Action A)                          │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │ additions: (                                              │  │
│  │   (task123, flowsInto, task456),                         │  │
│  │   (task123, flowsInto, task789)                          │  │
│  │ )                                                        │  │
│  │ deletions: ()                                            │  │
│  └───────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Data Flow: Pure Function Chain

```
INPUT                                                  OUTPUT
┌────────────┐                                    ┌────────────┐
│  RDF Graph │                                    │ QuadDelta  │
│  (Data)    │                                    │ (Action)   │
└─────┬──────┘                                    └─────▲──────┘
      │                                                 │
      │         ┌─────────────────────┐                │
      └────────►│  SPARQL Template    │────────────────┘
                │  (from ontology)    │
                │                     │
                │  μ: Operator        │
                │  O: Observation     │
                │  A: Action          │
                │                     │
                │  A = μ(O)           │
                └─────────────────────┘

Legend:
  O = RDF graph (observation)
  μ = SPARQL query engine (operator)
  A = QuadDelta (action)

Flow:
  1. Retrieve template μ from ontology
  2. Apply μ to observation O (graph)
  3. Produce action A (QuadDelta)
```

---

## 🏗️ Template Discovery & Execution

```
┌─────────────────────────────────────────────────────────────────┐
│                    ONTOLOGY (Knowledge Base)                     │
│                                                                  │
│  ┌────────────────┐  ┌────────────────┐  ┌────────────────┐   │
│  │   Topology     │  │    Dynamic     │  │    Shallow     │   │
│  │   Template     │  │   Template     │  │   Template     │   │
│  │   v1.0.0       │  │   v1.0.0       │  │   v1.0.0       │   │
│  └────────────────┘  └────────────────┘  └────────────────┘   │
│         │                    │                    │             │
│         └────────────────────┴────────────────────┘             │
│                              │                                  │
│                     Discoverable via SPARQL:                    │
│         SELECT * WHERE { ?t a kernel:ExecutionTemplate }        │
│                                                                  │
└───────────────────────────────┬─────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│                      KERNEL EXECUTOR                             │
│                                                                  │
│  User specifies: execution_template_uri                          │
│         │                                                        │
│         ▼                                                        │
│  Kernel retrieves template from ontology                         │
│         │                                                        │
│         ▼                                                        │
│  Kernel executes template (NO if/else)                          │
│         │                                                        │
│         ▼                                                        │
│  Return QuadDelta                                               │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🆕 Adding New Template: Zero Python Changes

```
BEFORE (Broken Pattern):
┌─────────────────────────────────────────────────────────────────┐
│                   KERNEL.COPY() (Mutable)                        │
│                                                                  │
│  if config.cardinality == "topology":                            │
│      query = "..."  # Topology logic                            │
│  elif config.cardinality == "dynamic":                           │
│      query = "..."  # Dynamic logic                             │
│  elif config.cardinality == "NEW_BEHAVIOR":  ◄─── EDIT CODE     │
│      query = "..."  # New logic              ◄─── EDIT CODE     │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘

AFTER (Pure Pattern):
┌─────────────────────────────────────────────────────────────────┐
│                   KERNEL.COPY() (Immutable)                      │
│                                                                  │
│  template = retrieve(config.execution_template_uri)              │
│  results = execute(template, bindings)                           │
│  return QuadDelta(results)                                      │
│                                                                  │
│  ✅ NEVER CHANGES ✅                                            │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│                      ONTOLOGY (Add Template)                     │
│                                                                  │
│  kernel:NewBehaviorTemplate  ◄──── ADD HERE (RDF only)          │
│    a kernel:ExecutionTemplate ;                                 │
│    kernel:sparqlTemplate """                                    │
│      SELECT ?p ?o WHERE { ... new logic ... }                   │
│    """ .                                                        │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔀 Multi-Template Execution Flow

```
                      ┌─────────────┐
                      │  RDF Graph  │
                      │  (Shared)   │
                      └──────┬──────┘
                             │
           ┌─────────────────┼─────────────────┐
           │                 │                 │
           ▼                 ▼                 ▼
    ┌──────────┐      ┌──────────┐      ┌──────────┐
    │ Topology │      │ Dynamic  │      │ Shallow  │
    │ Template │      │ Template │      │ Template │
    └─────┬────┘      └─────┬────┘      └─────┬────┘
          │                 │                 │
          ▼                 ▼                 ▼
    ┌──────────┐      ┌──────────┐      ┌──────────┐
    │ SPARQL   │      │ SPARQL   │      │ SPARQL   │
    │ Engine   │      │ Engine   │      │ Engine   │
    └─────┬────┘      └─────┬────┘      └─────┬────┘
          │                 │                 │
          ▼                 ▼                 ▼
    ┌──────────┐      ┌──────────┐      ┌──────────┐
    │  Delta1  │      │  Delta2  │      │  Delta3  │
    │ (struct) │      │ (dynamic)│      │ (shallow)│
    └──────────┘      └──────────┘      └──────────┘

Same Kernel.copy() executes all templates!
```

---

## 📊 Comparison: Code vs RDF Logic

```
BEFORE (Impure):
┌─────────────────────────────────────────────────────────────────┐
│                    PYTHON CODE (100+ lines)                      │
│                                                                  │
│  if cardinality == "topology":                                   │
│    ├─ Build query for topology                                  │
│    ├─ Filter metadata                                           │
│    └─ Return structural properties                              │
│  elif cardinality == "dynamic":                                  │
│    ├─ Build query for dynamic                                   │
│    ├─ Filter static properties                                  │
│    └─ Return runtime properties                                 │
│  elif cardinality == "shallow":                                  │
│    ├─ Build query for shallow                                   │
│    ├─ Filter deep references                                    │
│    └─ Return literals only                                      │
│                                                                  │
│  ❌ Business logic in code ❌ Must edit for new behavior        │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘

AFTER (Pure):
┌─────────────────────────────────────────────────────────────────┐
│                     PYTHON CODE (30 lines)                       │
│                                                                  │
│  template = retrieve(config.execution_template_uri)              │
│  results = execute(template, bindings)                           │
│  return QuadDelta(results)                                      │
│                                                                  │
│  ✅ Pure retrieval + execution ✅ NEVER changes                 │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│                   RDF ONTOLOGY (80+ lines)                       │
│                                                                  │
│  kernel:CopyTopologyTemplate                                    │
│    kernel:sparqlTemplate """ ... topology logic ... """ .        │
│                                                                  │
│  kernel:CopyDynamicTemplate                                     │
│    kernel:sparqlTemplate """ ... dynamic logic ... """ .         │
│                                                                  │
│  kernel:CopyShallowTemplate                                     │
│    kernel:sparqlTemplate """ ... shallow logic ... """ .         │
│                                                                  │
│  ✅ Business logic in RDF ✅ Queryable ✅ Versioned             │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🎯 The Semantic Singularity

```
              A = μ(O)

Where:
  O (Observation)  = RDF Graph with workflow data
  μ (Operator)     = SPARQL query template from ontology
  A (Action)       = QuadDelta (additions/deletions)

Implementation:
┌─────────────┐
│  O: Graph   │  ──┐
└─────────────┘    │
                   │    ┌────────────────────────┐
┌─────────────┐    ├───►│  SPARQL Engine         │
│ μ: Template │  ──┘    │  (Pure Operator)       │
└─────────────┘         └──────────┬─────────────┘
                                   │
                                   ▼
                         ┌─────────────────┐
                         │  A: QuadDelta   │
                         └─────────────────┘

✅ Python is just the VM
✅ All logic is RDF → SPARQL → RDF
✅ Validation (SHACL) and Execution (SPARQL) are both RDF-native
```

---

## 🏆 Pattern Guarantees

```
┌─────────────────────────────────────────────────────────────────┐
│                   ARCHITECTURAL GUARANTEES                       │
│                                                                  │
│  1. IMMUTABLE KERNEL                                            │
│     Kernel.copy() NEVER changes after implementation            │
│                                                                  │
│  2. RDF-ONLY LOGIC                                              │
│     All business logic in SPARQL templates (ontology)            │
│                                                                  │
│  3. ZERO PYTHON IF/ELSE                                         │
│     No parameter interpretation in Python code                   │
│                                                                  │
│  4. DISCOVERABLE BEHAVIORS                                      │
│     Query ontology to find all templates at runtime              │
│                                                                  │
│  5. SEMANTIC VERSIONING                                         │
│     Templates have version numbers (1.0.0, 2.0.0, etc.)         │
│                                                                  │
│  6. SHACL-COMPATIBLE                                            │
│     Templates can be validated by SHACL shapes                   │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

**Visual documentation completed. Pattern is fully specified and verified.**
